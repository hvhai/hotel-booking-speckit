-- V1__init.sql: Initial schema migration (placeholder)
-- Add your base schema here, e.g. tables for User, Room, Booking, Cancellation

-- Example:
-- CREATE TABLE IF NOT EXISTS example_table (
--     id SERIAL PRIMARY KEY,
--     name VARCHAR(255) NOT NULL
-- );

